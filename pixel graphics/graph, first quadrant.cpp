#include<graphics.h>
#include <winbgim.h>
//#include <conio.h>

/*
-------------------------------------------------------------------------------------------
 @author Booh-rm
-------------------------------------------------------------------------------------------
*/

int main()
{
	initwindow(800,800,"Pixels");
 
 	cleardevice();
 	
 	//-------------------------------------------------------------------
 	
	for (int a=1; a<=460; a++){
		putpixel(170,50+a,BLUE);
	}
	for	(int b=1; b<=430; b++){
		putpixel(160+b,500,BLUE);
	}
	
 	//-------------------------------------------------------------------
	
	for (int d=1; d<=444; d++){
		putpixel(200-((2/30.9*d)),500-d,RED);
	}
	for (int e=1; e<=432; e++){
		putpixel(240-((2/12.5*e)),500-e,RED);
	}
	for (int f=1; f<=391; f++){
		putpixel(280-((2/7.2*f)),500-f,RED);
	}
	for (int g=1; g<=344; g++){
		putpixel(320-((2/4.64*g)),500-g,RED);
	}
	for (int h=1; h<=280; h++){
		putpixel(360-((2/2.98*h)),500-h,RED);
	}
	for (int i=1; i<=222; i++){
		putpixel(400-((2/1.94*i)),500-i,RED);
	}
	for (int j=1; j<=163; j++){
		putpixel(420-((3/1.82*j-20)),500-j,RED);
	}
	for (int k=1; k<=111; k++){
		putpixel(490-((7/2.3*k-20)),500-k,RED);
	}
	for (int l=1; l<=65; l++){
		putpixel(560-6*l,500-l,RED);
	}
	for (int c=1; c<=30; c++){
		putpixel(570-((7/0.50*c-20)),500-c,RED);
	}
	
	for	(int m=1; m<=29; m++){
		putpixel(570-((2/0.14*m-20)),500-m,RED);
	}
	
	//-------------------------------------------------------------------
	
	getch();
 
	return 0;
}
